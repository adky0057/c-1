
#include <stdio.h>
int max(int num1, int num2);
int main(void)
{
	int a, b, result;
	scanf("%d %d",&a ,&b);
	result = max(a, b);
	printf("%d\n", result);
	return 0;
}

int max(int num1, int num2) {
	if(num1 > num2)
	return num1;
	else
	return num2;
}