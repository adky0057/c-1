#include <stdio.h>

int addNumbers(int p1, int p2) {
	return p1 + p2; }

int main() 
{
	int num1, num2;
	int sum;
	scanf("%d, %d",&num1, &num2);	
	
	sum = addNumbers(num1, num2);

	printf("%d + %d =%d \n", num1, num2, sum);
	return 0;
}
    